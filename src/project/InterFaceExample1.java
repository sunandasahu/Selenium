package project;

public interface InterFaceExample1 // we are going to extend this interface in other interface then implement in a class
{
	String name="Hima";//variables are by default public, static,and final in interface.They must be 
	int age=15;        //initializes at the time of declaration.
	public void dis();//by default this method is abstract
	
	
}
